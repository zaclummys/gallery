{
    "private": true,
    "devDependencies": {
        "parcel-bundler": "^1.11.0",
        "postcss-modules": "^1.4.1",
        "postcss-preset-env": "^6.5.0"
    },
    "dependencies": {
        "react": "^16.7.0",
        "react-dom": "^16.7.0"
    },
    "scripts": {
        "dev": "yarn run development",
        "prod": "yarn run production",
        "development": "yarn run parcel ./src/index.html",
        "production": "yarn run parcel build ./src/index.html"
    }
}
